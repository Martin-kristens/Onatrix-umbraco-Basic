@using Umbraco.Cms.Web.Common.PublishedModels;
@using Umbraco.Extensions;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@{

    Layout = "mainLayout.cshtml";
    ViewBag.Title = Model?.Value("pageTitle") ?? null;



    var backgroundImage = Model?.Value<IPublishedContent>("heroBackgroundImage")?.Url() ?? "";

    var signatureImage = Model?.Value<IPublishedContent>("aboutSignatureImage")?.Url() ?? "";

    var cardImage = Model?.Value<IPublishedContent>("ourServicesCardImage")?.Url() ?? "";	
}

 <section id="hero" class="hero bg-gray" >
	<div class="hero-background" style="background-image: url('@backgroundImage'); background-size: cover; background-position: 60%"></div>
	<div class="container">
		<!-- <img src="./images/sl-home2.svg" alt=""> -->
		<h1>@Model?.Value("heroHeadline")</h1>
		<p>@Model?.Value("heroText")</p>
		<button class="btn-primary" href=@Model?.Value("heroButtonUrl")>@Model?.Value("heroButtonText")</button>
	</div>
</section>

 <section id="about-us">
	<div class="container">
		<div class="left-side">
			<h6>@Model.Value("aboutSectionTitle")</h6>
			<h2>@Model.Value("aboutHeadline")</h2>
			<p>@Model.Value("aboutQuote")</p>
			<p>@Model.Value("aboutText")</p>
			<img src="@signatureImage" alt="@Model.Value("aboutSignature")">
			<p>@Model.Value("aboutSignature")</p>
		</div>
	@* 	<form class="right-side">
			<h5 class="bg-primary">Request a call back</h5>
			<p>Dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur excepteur sint occaecat</p>
			<input type="text" placeholder="Name">
			<input type="text" placeholder="Email address">
			<input type="text" placeholder="Phone">
			<select name="" id="">
				<option value="">Financial consulting</option>
				<option value="">2</option>
				<option value="">3</option>
			</select>
			<button class="btn-accent">Submit</button>
		</form> *@	
	</div>
</section>

  <section id="our-services" class="bg-gray">
                <div class="container">
                    <h6>@Model.Value("ourServicesPageTitle")</h6>
                    <h2>@Model.Value("ourServicesHeadline")</h2>

                    <div class="our-services-cards">
      
                        <div class="our-services-card">
                            <h4 class="bg-light">@Model.Value("ourServicesCardHeadline")</h4>
                            <img src="@cardImage" alt="">
                            <p class="bg-light">@Model.Value("ourServicesCardText")</p>
                        </div>
                        <div class="our-services-card">
                            <h4 class="bg-light">@Model.Value("ourServicesCardHeadline")</h4>
                            <img src="@cardImage" alt="">
                            <p class="bg-light">@Model.Value("ourServicesCardText") </p>
                        </div>
                        <div class="our-services-card"> 
                            <h4 class="bg-light">@Model.Value("ourServicesCardHeadline")</h4>
                            <img src="cardImage" alt="">
                            <p class="bg-light">@Model.Value("ourServicesCardText") </p>
                        </div>
                        <div class="our-services-card">
                            <h4 class="bg-light">@Model.Value("ourServicesCardHeadline")</h4>
                            <img src="cardImage" alt="">
                            <p class="bg-light">@Model.Value("ourServicesCardText") </p>
                        </div>
                        <div class="our-services-card">
                            <h4 class="bg-light">@Model.Value("ourServicesCardHeadline")</h4>
                            <img src="./images/card-5.svg" alt="">
                            <p class="bg-light">@Model.Value("ourServicesCardText")</p>
                        </div>
                        <div class="our-services-card">
                            <h4 class="bg-light">@Model.Value("ourServicesCardHeadline")</h4>
                            <img src="cardImage" alt="">
                            <p class="bg-light">@Model.Value("ourServicesCardText")</p>
                        </div>
                    </div>
                
                </div>           
            </section>